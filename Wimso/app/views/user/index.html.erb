<h1>Listing posts</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @allusers.each do |user| %>
  <tr>
    <td><%= user.email %></td>
	
	<td><%=link_to "Profile", show_profile_path(user.id)%></td>
  </tr>
<% end %>
</table>

<br />
