<% u=User.find(params[:id])%>

<% if current_user.id == u.id  %>
Hallo 
<%= u.name %> <br/>
Account settings: </br>
<%= u.email %><br/>
<%= u.privacy_flag %> <br/>
My Series<br/>
<% u.user_run.each do |myrun|%>
<%dieid= myrun.run_id %>
<%=Run.find(dieid).name %>
<%= link_to 'edit', edit_ur_path(myrun) %> | <%= link_to 'delete', delete_ur_path(myrun), confirm: 'Are you sure?', method: :delete %>
<br/>
<br/>
<% end %>
Add a Series that is not in the global list</br>
<%= link_to 'Add new Series', new_run_path %>
<br/>

<br/>
<br/>
<br/>

Friends: </br>
	<% u.friends.each do |friend| %>
		<%= friend.email %> </br>
	<% end %>

<% else %>
<%= form_tag(add_friend_path, :method => "put") do %>
 <%= hidden_field_tag :id, u.id %>
  <%= submit_tag("add") %>
<% end %>
not your Profile


<% end %>
